# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets — provider tokens live in env / an untracked .env (never committed).
# uv loads .env into `uv run` via --env-file/UV_ENV_FILE. The .env.example template IS tracked.
.env
.env.*
!.env.example

# Generated artifacts
outputs/

# Local model weights (downloaded via `hf`, kept out of git)
models/
*.safetensors
*.ckpt
*.gguf
*.pt
*.bin

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
